# Kafka
confluent-kafka==2.4.0

# Spark + Delta Lake (for local / non-Docker runs)
pyspark==3.5.0
delta-spark==3.0.0

# Schema validation
jsonschema==4.22.0

# Testing
pytest==8.2.2
pytest-mock==3.14.0
